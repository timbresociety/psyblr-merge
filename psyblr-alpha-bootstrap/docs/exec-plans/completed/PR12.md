# PR12 — Alpha hardening

Checkpoint schema v8 preserves opponent-camp handoff and selected steal state, and deliberately migrates valid v1–v7 progress without reset. Added Raid cell/mirroring and defense validation unit coverage.

Verified: `npm run validate:content`, `npm run typecheck`, `npm run test`, and `npm run build`. No live Supabase environment was configured, so Edge Function/RPC integration, anonymous auth, PWA strategy, and full Playwright visual flow remain unverified.
